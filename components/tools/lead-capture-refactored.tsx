// This is a wrapper that re-exports the refactored LeadCaptureTool component
// Import from the folder structure
import LeadCaptureTool from "./lead-capture";

// Export the component for use elsewhere in the app
export default LeadCaptureTool;
